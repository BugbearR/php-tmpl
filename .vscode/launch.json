{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "php",
      "request": "launch",
      "name": "Listen for Xdebug",
      "hostname": "workspace",
      "port": 9003,
      "pathMappings": {
        "/var/www/html": "${workspaceFolder}/public"
      }
    }
  ]
}
